[soc_harness]
is_custom = 1
label = Send to SPARKSOC Harness
description = Deliver the full alert result set to the SPARKSOC agent harness, HMAC-signed with replay protection.
icon_path = sparksoc.png
payload_format = json
# The harness handles its own dedupe; let every firing through and let the
# harness fingerprint decide. ttl covers the results file lifetime.
ttl = 300

param.harness_url = http://10.90.1.40:8080/v1/alert
param.shared_secret =
param.verify_tls = 1
param.ca_bundle =
param.timeout = 30
param.severity =
param.urgency =
param.labels =
